import Fastify from 'fastify'; import { resolve } from 'node:path'; import { RegionService } from './domain/region/application/region-service.js'; import { GameStore } from './domain/game/dao/game-store.js'; import { GameService } from './domain/game/application/game-service.js'; import { routes } from './domain/game/api/routes.js';
const app=Fastify({logger:false}),regions=new RegionService(process.env.REGION_DATA_DIR??resolve(process.cwd(),'data')),game=new GameService(new GameStore(process.env.RAID_DB_PATH??resolve(process.cwd(),'data','walkwar.sqlite')),regions);routes(app,game,regions);const timer=setInterval(()=>game.tick(),250);const shut=async()=>{clearInterval(timer);await app.close();game.close()};process.on('SIGINT',()=>void shut());process.on('SIGTERM',()=>void shut());await app.listen({host:process.env.HOST??'127.0.0.1',port:+(process.env.PORT??3040)});
